mod actions {
    mod proposal_actions;
    mod vote_actions;
}
mod models {
    mod global;
    mod option_summary;
    mod proposal;
    mod vote;
}

mod utils {}
mod constants;

#[cfg(test)]
mod tests;
